@import url(<%= asset_url('foundation.css') %>);

 header {
   border-bottom: 1px solid lightgray;
 }

 header h2::before {
   content: url(<%= image_url('owl.jpg') %>);
   height: 50px;
 }

 h3.success {
   color: "green"
 }

 .small {
   color: gray;
   font-size: smaller;
 }

 /*header, header h2 {
  height: 15vh;
 }

 main {
   clear:both;
 }*/
