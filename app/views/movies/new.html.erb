<% if @movie.errors.any? %>
  <ul class="errors">
    <% @movie.errors.each do |column, message| %>
      <li>
        <strong><%= column.capitalize %></strong> <%= message %>
      </li>
    <% end %>
  </ul>
<% end %>
<h3>Add a new movie</h3>
asdf
<%= render partial: "form", locals: { action_name: "add a movie", form_method: :post } %>
