<h3>All albums: </h3>
<table>
  <tr>
    <th> Votes  </th>
    <th> Title</th>
    <th>Created by</th>
    <th> Published </th>
    <th>  Upvote  </th>
  </tr>
  <%@all_albums.each do |item|%>
    <%= render partial: 'work_table', locals:{item:item}%>
  <%end%>
</table>
<%= button_to 'View all media', welcome_index_path, method: :get %>
<%= button_to 'Add a new album', new_album_path, method: :get %>
