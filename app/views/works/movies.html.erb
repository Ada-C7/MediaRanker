<header>
  <%= render "shared/header" %>
</header>

<main>
  <section>
    <h2>Movies</h2>
    <ul>
    <% Work.all.where(category: "movie").each do |movie| %>
      <li><%= movie.title %></li>
    <% end %>
    </ul>
  </section>

  <nav>
    <%= link_to "All Media", homepage_path %>
    <%= link_to "Add Movie", new_movie_path %>
  </nav>

</main>
