<h1>All Movies</h1>

<% @movies.each do |movie| %>
  <h3> <%= link_to movie.title, work_path(movie.id) %> by <%= movie.creator %> </h3>
  <h4> <%= movie.votes.count %> votes </h4>
<% end %>

<%= link_to "Create new Movie", new_work_path(:category => "movie"), class: "button" %>
