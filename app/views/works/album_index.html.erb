<h2> Book Only </h2>

<ul>
  <% @works.each do |work| %>
  <li>
    <%= "Title: #{work.title}" %>
    <%= "Created By: #{work.creator}" %>
    <%= "Published: #{work.pub_date}" %>
  </li>
  <% end %>
</ul>
