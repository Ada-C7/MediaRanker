<header>
  <h1> Add New Work </h1>
</header>

<main>
  <%= render partial: "form", locals: { submit_text: "Add New"} %>

  <% if @work.errors.any? %>
    <ul>
      <% @work.errors.each do |column, message| %>
        <li>
          <%= column.capitalize %> <%= message %>
        </li>
      <% end %>
    </ul>
  <% end %>
</main>
