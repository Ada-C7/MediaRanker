<h2> Album Only </h2>

<ul>
  <li>
    <%= "Title: #{@work.title}" %>
    <%= "Created By: #{@work.creator}" %>
    <%= "Published: #{@work.pub_date}" %>
  </li>
</ul>
<%= button_to "Edit Album", edit_work_path, method: :get %>
<%= link_to "Back to All Albums", albums_path %>
