<h1>List of Albums</h1>
<p>
  <% @albums.each do |a| %>
  <ul>
    <%= link_to a.title, work_path(a.id) %>, <%= a.creator %>, <%= a.publication_year %>
  </ul>
  <% end %>
</p>

<%= link_to "View all media", root_path %>
<%= link_to "Add a new album", new_album_path %>
