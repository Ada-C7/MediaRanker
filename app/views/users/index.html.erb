<h1>Users</h1>

<ul>
     <% @users.each do | user | %>
          <li> <%= link_to user.username, user_path(user.id) %> </li>
          <li> <%= user.votes.size %> </li>
          <li> <%= user.created_at %> </li>
     <% end %>
</ul>

<%= link_to 'Back', root_path %>
