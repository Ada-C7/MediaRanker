<section id="content">
  <h2>All Users!</h2>

  <table>
    <tr>
      <th>Name</th>
    </tr>
    <% @users.each do | user | %>
    <tr>
      <td><%= link_to user.username, user_path(user.id) %></td>
    </tr>
    <% end %>
  </table>
</section>
